# Job Application Tracker

A web-based application to track your job applications with status management, search/filter capabilities, and a clean dashboard.

## Quick Start

```bash
# Clone or create project directory
mkdir job-application-tracker && cd job-application-tracker

# Install dependencies
pip install -r requirements.txt

# Run the app
python app.py
